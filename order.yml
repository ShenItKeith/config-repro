server:
  port: 8080
spring:
  application:
    name: keith-eureka
eureka:
  instance:
    hostname: keith-eureka
  client:
    service-url:
      defaultZone:  http://localhost:8080/eureka
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
